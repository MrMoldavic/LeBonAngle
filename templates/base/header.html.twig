<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">

        <a class="navbar-brand" href="{{ path('home') }}">Le BonAngle</a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">

            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
             {% if app.user %}
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">Messages</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Annonces
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('article_create') }}">Créer Annonce</a></li>
                        <li><a class="dropdown-item" href="{{ path('articles') }}">Voir ses Annonces</a></li>
                    </ul>
                </li>
                {% endif %}
                {% if is_granted("ROLE_ADMIN") %}
                <li class="nav-item dropdown">
                     <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Catégories
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('category_create') }}">Créer une Catégorie</a></li>
                        <li><a class="dropdown-item" href="{{ path('categories') }}">Voir les catégories</a></li>
                    </ul>
                </li>
                 <li class="nav-item dropdown">
                     <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        Villes
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('city_create') }}">Importer une Ville</a></li>
                        <li><a class="dropdown-item" href="{{ path('cities') }}">Voir les villes</a></li>
                    </ul>
                </li>
                {% endif %}
                </ul>

            <form class="d-flex" role="search">
                <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
                <button class="btn btn-outline-success" type="submit">Search</button>
            </form>

            {% if app.user %}
                <div class="nav-item">
                    <a class="nav-link" href="{{ path('profil') }}">Profil</a>
                </div>
                <div class="nav-item">
                    <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
                </div>
            {% else %}
                <div class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Login</a>
                </div>

                <div class="nav-item">
                    <a class="nav-link" href="{{ path('app_register') }}">Register</a>
                </div>
            {% endif %}
        </div>
    </div>
    
</nav>

<form method="post" accept-charset="UTF-8">

<div class="d-flex justify-content-center p-2">

 <div class="col-1 m-1 ">

    
    <select class="form-select">
        {% if categories is defined and categories is not null%}
            <option value="NULL">Choisir...</option>
            {% for category in categories %}
                <option>{{ category.name }}</option>
            {% endfor %}
        {% else %}
            Pas de données
        {% endif %}
    </select>
 </div>

  <div class="col-1 m-1">
    <input type="text" class="form-control" placeholder="Recherche..." aria-label="First name">
  </div>

  <div class="col-1 m-1">
        <select class="form-select">
        {% if cities is defined and cities is not null%}
            <option value="NULL">Localisation...</option>
            {% for city in cities %}
                <option>{{ city.name }}</option>
            {% endfor %}
        {% else %}
            Pas de données
        {% endif %}
    </select>
  </div>
  <div class="col-1 m-1">
    <input type="text" class="form-control" placeholder="Prix min" aria-label="First name">
  </div>
  <div class="col-1 m-1">
    <input type="text" class="form-control" placeholder="Prix max" aria-label="First name">
  </div>
  <div class="col-1 m-1">
    <button type="submit" class="btn btn-primary">Rechercher</button>
  </div>
</form>



  
</div>
