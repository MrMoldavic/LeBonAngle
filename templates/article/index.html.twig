{% extends 'base.html.twig' %}

{% block body %}

{% if articles %}
    <ul>
    {% for article in articles %}
        <li><a href="/article_edit/{{article.id }}">{{ article.title }}</a></li>
    {% endfor %}
    </ul>
{% else %}
    Pas de données
{% endif %}

{% endblock %}

